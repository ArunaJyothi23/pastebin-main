services:
  - type: web
    name: pastebin-lite-express
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm run start
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: NODE_ENV
        value: production
      - key: TEST_MODE
        value: "1"
